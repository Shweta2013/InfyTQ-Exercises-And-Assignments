SELECT R.LOCATION, C.CUSTTYPE, SUM(P.QUANTITY ) "TOTALQUANTITY"
FROM RETAILOUTLET R, PURCHASEBILL P, CUSTOMER C, ITEM I WHERE R.ROID=P.ROID AND P.CUSTID=C.CUSTID AND I.ITEMCODE=P.ITEMCODE AND C.GENDER='M' AND I.ITEMTYPE IN ('Apparels', 'Computer')
GROUP BY R.LOCATION, C.CUSTTYPE